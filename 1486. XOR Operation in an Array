1486. XOR Operation in an Array

Input: n = 5, start = 0
Output: 8
Explanation: Array nums is equal to [0, 2, 4, 6, 8] where (0 ^ 2 ^ 4 ^ 6 ^ 8) = 8.
Where "^" corresponds to bitwise XOR operator.

class Solution {
public:
    int xorOperation(int n, int start) {
        vector<int>nums;
        nums.reserve(1000);
        int result=0;
        for(int i=0;i<n;i++)
         nums[i]= start + 2 * i;
        result = nums[0];
        for(int i=1;i<n;i++)
            result = result ^ nums[i];
        return result;
    }
};

/*Things learnt
**Logical XOR operation is done with ^ this symbol and learnt about reserve function to reserve memory for vectors as memory is allocated at runtime.
*/
